<% if !@posts.empty?%>
<table>
  <thead>
    <tr>
      <th>Match</th>
      <th>Content</th>
      <th>from_id</th>
      <th>to_id</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.match_id %></td>
        <td><%= post.content %></td>
        <td><%= post.from_id %></td>
        <td><%= post.to_id %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else%>
주고받은 쪽지가 없습니다!
<% end%>

<%= render 'form', post: @post %>