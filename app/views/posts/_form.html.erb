<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    <%= form.hidden_field :match_id, id: :post_match_id,value: @match_id %>
  </div>

  <div class="field">

    <%= form.hidden_field :from_id, id: :post_from_id,value: @user_id %>
  </div>
  
  <div class="field">

    <%= form.hidden_field :to_id, id: :post_to_id,value: @likee_id %>
  </div>
<div class="input-group">

    <%= form.text_area :content, id: :post_content, class:"form-control"%>
  

  <div class="actions input-group-append">
    <%= form.submit '전송', class:"btn btn-outline-secondary"%>
  </div>
</div>
<% end %>
